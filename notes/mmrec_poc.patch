diff --git a/src/configs/model/LightGCN.yaml b/src/configs/model/LightGCN.yaml
index 1c60873..400236c 100644
--- a/src/configs/model/LightGCN.yaml
+++ b/src/configs/model/LightGCN.yaml
@@ -1,6 +1,5 @@
 embedding_size: 64
 is_multimodal_model: False
-n_layers: [1, 2, 3, 4]
-reg_weight: [1e-02, 1e-03, 1e-04, 1e-05, 1e-06]
-hyper_parameters: ["n_layers", "reg_weight"]
-
+n_layers: 1
+reg_weight: 1e-05
+hyper_parameters: []
diff --git a/src/configs/model/MMGCN.yaml b/src/configs/model/MMGCN.yaml
index a97a81a..58674ea 100644
--- a/src/configs/model/MMGCN.yaml
+++ b/src/configs/model/MMGCN.yaml
@@ -1,7 +1,6 @@
 embedding_size: 64
-n_layers: 2
-reg_weight: [0, 0.00001, 0.0001, 0.001, 0.01, 0.1]
-learning_rate: [0.0001, 0.0005, 0.001, 0.005, 0.01]
-
-hyper_parameters: ["reg_weight", "learning_rate"]
+n_layers: 1
+reg_weight: 0.00001
+learning_rate: 0.001
 
+hyper_parameters: []
diff --git a/src/configs/model/VBPR.yaml b/src/configs/model/VBPR.yaml
index 1ea6fa5..501876c 100644
--- a/src/configs/model/VBPR.yaml
+++ b/src/configs/model/VBPR.yaml
@@ -1,3 +1,3 @@
 embedding_size: 64
-reg_weight: [2.0, 1.0, 1e-01, 1e-02, 1e-03, 1e-04, 1e-05]
-hyper_parameters: ["reg_weight"]
+reg_weight: 1e-05
+hyper_parameters: []
diff --git a/src/main.py b/src/main.py
index 77e6f48..aa2a085 100644
--- a/src/main.py
+++ b/src/main.py
@@ -13,6 +13,46 @@ from utils.quick_start import quick_start
 os.environ['NUMEXPR_MAX_THREADS'] = '48'
 
 
+def parse_value(raw):
+    if isinstance(raw, str) and ',' in raw:
+        return [parse_value(part.strip()) for part in raw.split(',')]
+    if raw in {'True', 'true'}:
+        return True
+    if raw in {'False', 'false'}:
+        return False
+    try:
+        return int(raw)
+    except ValueError:
+        pass
+    try:
+        return float(raw)
+    except ValueError:
+        return raw
+
+
+def parse_overrides(items):
+    overrides = {}
+    idx = 0
+    while idx < len(items):
+        item = items[idx]
+        if not item.startswith('--'):
+            idx += 1
+            continue
+        key = item[2:]
+        if '=' in key:
+            key, value = key.split('=', 1)
+            overrides[key] = parse_value(value)
+            idx += 1
+            continue
+        if idx + 1 < len(items) and not items[idx + 1].startswith('--'):
+            overrides[key] = parse_value(items[idx + 1])
+            idx += 2
+        else:
+            overrides[key] = True
+            idx += 1
+    return overrides
+
+
 if __name__ == '__main__':
     parser = argparse.ArgumentParser()
     parser.add_argument('--model', '-m', type=str, default='SELFCFED_LGN', help='name of models')
@@ -22,8 +62,8 @@ if __name__ == '__main__':
         'gpu_id': 0,
     }
 
-    args, _ = parser.parse_known_args()
+    args, unknown_args = parser.parse_known_args()
+    config_dict.update(parse_overrides(unknown_args))
 
     quick_start(model=args.model, dataset=args.dataset, config_dict=config_dict, save_model=True)
 
-
diff --git a/src/models/lightgcn.py b/src/models/lightgcn.py
index 4b5497d..e2eb775 100644
--- a/src/models/lightgcn.py
+++ b/src/models/lightgcn.py
@@ -75,15 +75,16 @@ class LightGCN(GeneralRecommender):
             Sparse tensor of the normalized interaction matrix.
         """
         # build adj matrix
-        A = sp.dok_matrix((self.n_users + self.n_items,
-                           self.n_users + self.n_items), dtype=np.float32)
         inter_M = self.interaction_matrix
         inter_M_t = self.interaction_matrix.transpose()
-        data_dict = dict(zip(zip(inter_M.row, inter_M.col+self.n_users),
-                             [1]*inter_M.nnz))
-        data_dict.update(dict(zip(zip(inter_M_t.row+self.n_users, inter_M_t.col),
-                                  [1]*inter_M_t.nnz)))
-        A._update(data_dict)
+        rows = np.concatenate([inter_M.row, inter_M_t.row + self.n_users])
+        cols = np.concatenate([inter_M.col + self.n_users, inter_M_t.col])
+        data = np.ones(len(rows), dtype=np.float32)
+        A = sp.coo_matrix(
+            (data, (rows, cols)),
+            shape=(self.n_users + self.n_items, self.n_users + self.n_items),
+            dtype=np.float32,
+        )
         # norm adj matrix
         sumArr = (A > 0).sum(axis=1)
         # add epsilon to avoid Devide by zero Warning
diff --git a/src/models/mmgcn.py b/src/models/mmgcn.py
index 1fcbc01..d310388 100644
--- a/src/models/mmgcn.py
+++ b/src/models/mmgcn.py
@@ -206,11 +206,11 @@ class BaseModel(MessagePassing):
         x = torch.matmul(x, self.weight)
         return self.propagate(edge_index, size=(x.size(0), x.size(0)), x=x)
 
-    def message(self, x_j, edge_index, size):
+    def message(self, x_j):
         return x_j
 
     def update(self, aggr_out):
         return aggr_out
 
     def __repr(self):
-        return '{}({},{})'.format(self.__class__.__name__, self.in_channels, self.out_channels)
\ No newline at end of file
+        return '{}({},{})'.format(self.__class__.__name__, self.in_channels, self.out_channels)
diff --git a/src/utils/metrics.py b/src/utils/metrics.py
index d76d09e..718fc0c 100644
--- a/src/utils/metrics.py
+++ b/src/utils/metrics.py
@@ -48,13 +48,13 @@ def ndcg_(pos_index, pos_len):
     len_rank = np.full_like(pos_len, pos_index.shape[1])
     idcg_len = np.where(pos_len > len_rank, len_rank, pos_len)
 
-    iranks = np.zeros_like(pos_index, dtype=np.float)
+    iranks = np.zeros_like(pos_index, dtype=float)
     iranks[:, :] = np.arange(1, pos_index.shape[1] + 1)
     idcg = np.cumsum(1.0 / np.log2(iranks + 1), axis=1)
     for row, idx in enumerate(idcg_len):
         idcg[row, idx:] = idcg[row, idx - 1]
 
-    ranks = np.zeros_like(pos_index, dtype=np.float)
+    ranks = np.zeros_like(pos_index, dtype=float)
     ranks[:, :] = np.arange(1, pos_index.shape[1] + 1)
     dcg = 1.0 / np.log2(ranks + 1)
     dcg = np.cumsum(np.where(pos_index, dcg, 0), axis=1)
@@ -78,10 +78,10 @@ def map_(pos_index, pos_len):
         \end{align*}
     """
     pre = pos_index.cumsum(axis=1) / np.arange(1, pos_index.shape[1] + 1)
-    sum_pre = np.cumsum(pre * pos_index.astype(np.float), axis=1)
+    sum_pre = np.cumsum(pre * pos_index.astype(float), axis=1)
     len_rank = np.full_like(pos_len, pos_index.shape[1])
     actual_len = np.where(pos_len > len_rank, len_rank, pos_len)
-    result = np.zeros_like(pos_index, dtype=np.float)
+    result = np.zeros_like(pos_index, dtype=float)
     for row, lens in enumerate(actual_len):
         ranges = np.arange(1, pos_index.shape[1]+1)
         ranges[lens:] = ranges[lens - 1]
